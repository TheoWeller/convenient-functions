//Use reducers when you want to reduce the values of an arry into one single value or object.

// Reminder: '.sort' mutates the array its sorting
//Sorts by ascending order (smallest to largest)
array.sort((a, b) => a - b);
